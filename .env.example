# ========================================
# CONFIGURAÇÃO DO BANCO DE DADOS
# ========================================
# Copie este arquivo para .env e preencha com suas credenciais

# Para PostgreSQL Local:
DATABASE_URL=postgresql://usuario:senha@localhost:5432/rastreabilidade

# Para Render.com (exemplo):
# DATABASE_URL=postgresql://user:password@dpg-xxxxx.oregon-postgres.render.com/database_name

# ========================================
# NOTAS IMPORTANTES:
# ========================================
# 1. O arquivo .env NÃO deve ser commitado no Git (já está no .gitignore)
# 2. A URL deve seguir o formato: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
# 3. Certifique-se de que o banco PostgreSQL existe antes de rodar a aplicação
# 4. Use o script init_db.py para criar as tabelas após configurar o .env
